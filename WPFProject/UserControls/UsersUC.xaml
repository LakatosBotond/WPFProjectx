<UserControl x:Class="WPFProject.UserControls.UsersUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFProject.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel Margin="20">
            <TextBlock Text="Felhasználók" FontSize="30" FontWeight="Bold" Margin="0,0,0,20"/>
            <WrapPanel Margin="0,0,0,15">
                <TextBox x:Name="txtUsername" Width="120" Height="30" Margin="5"/>
                <TextBox x:Name="txtEmail" Width="180" Height="30" Margin="5"/>
                <TextBox x:Name="txtPassword" Width="120" Height="30" Margin="5"/>
                <TextBox x:Name="txtFirstName" Width="120" Height="30" Margin="5"/>
                <TextBox x:Name="txtLastName" Width="120" Height="30" Margin="5"/>
                <Button x:Name="btnAdd" Content="Hozzáadás" Width="100" Margin="5" Click="btnAdd_Click"/>
                <Button x:Name="btnUpdate" Content="Módosítás" Width="100" Margin="5" Click="btnUpdate_Click"/>
                <Button x:Name="btnDelete" Content="Törlés" Width="100" Margin="5" Click="btnDelete_Click"/>
            </WrapPanel>
            <DataGrid x:Name="usersGrid" Height="450" SelectionChanged="usersGrid_SelectionChanged"/>
        </StackPanel>
    </Grid>
</UserControl>
