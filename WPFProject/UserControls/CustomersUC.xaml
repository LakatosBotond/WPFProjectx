<UserControl x:Class="WPFProject.UserControls.CustomersUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFProject.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel Margin="20">
            <TextBlock Text="Vásárlók" FontSize="30" FontWeight="Bold" Margin="0,0,0,20"/>
            <WrapPanel Margin="0,0,0,15">
                <TextBox x:Name="txtUserId" Width="80" Height="30" Margin="5"/>
                <TextBox x:Name="txtPhone" Width="120" Height="30" Margin="5"/>
                <TextBox x:Name="txtAddress" Width="150" Height="30" Margin="5"/>
                <TextBox x:Name="txtCity" Width="120" Height="30" Margin="5"/>
                <TextBox x:Name="txtCountry" Width="120" Height="30" Margin="5"/>
                <TextBox x:Name="txtBirthDate" Width="120" Height="30" Margin="5" ToolTip="YYYY-MM-DD"/>
                <Button x:Name="btnAdd" Content="Hozzáadás" Width="100" Margin="5" Click="btnAdd_Click"/>
                <Button x:Name="btnUpdate" Content="Módosítás" Width="100" Margin="5" Click="btnUpdate_Click"/>
                <Button x:Name="btnDelete" Content="Törlés" Width="100" Margin="5" Click="btnDelete_Click"/>
            </WrapPanel>
            <DataGrid x:Name="customersGrid" Height="450" SelectionChanged="customersGrid_SelectionChanged"/>
        </StackPanel>
    </Grid>
</UserControl>
