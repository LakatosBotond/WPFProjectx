<Window
    x:Class="WPFProject.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ShopManager"
    Height="700"
    Width="1100"
    WindowStartupLocation="CenterScreen"
    Background="#F3F4F6">

    <DockPanel>

        <Border
            DockPanel.Dock="Top"
            Height="60"
            Background="#2563EB">

            <TextBlock
                Text="Shop Manager Admin Panel Asd"
                Foreground="White"
                FontSize="28"
                FontWeight="Bold"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"/>
        </Border>

        <Border
            DockPanel.Dock="Left"
            Width="220"
            Background="#1E293B">

            <ListView
                Background="Transparent"
                BorderThickness="0"
                Foreground="White"
                FontSize="20">

                <ListViewItem Content="Dashboard" x:Name="Dashboard" MouseLeftButtonUp="Dashboard_MouseLeftButtonUp"/>
                <ListViewItem Content="Felhasználók" x:Name="Users" MouseLeftButtonUp="Users_MouseLeftButtonUp"/>
                <ListViewItem Content="Vásárlók" x:Name="Costumers" MouseLeftButtonUp="Costumers_MouseLeftButtonUp"/>
                <ListViewItem Content="Termékek" x:Name="Products" MouseLeftButtonUp="Products_MouseLeftButtonUp"/>
                <ListViewItem Content="Beállítások" x:Name="Settings" MouseLeftButtonUp="Settings_MouseLeftButtonUp"/>
                <ListViewItem Content="Kilépés" x:Name="kilepesMenu" MouseLeftButtonUp="kilepesMenu_MouseLeftButtonUp"  />

            </ListView>

        </Border>

        <Grid x:Name="ContentGrid" Background="White">

            <StackPanel
                VerticalAlignment="Center"
                HorizontalAlignment="Center">

                <TextBlock
                    Text="ShopManager rendszer"
                    FontSize="34"
                    FontWeight="Bold"
                    Foreground="#1E293B"
                    Margin="0,0,0,20"/>

            </StackPanel>

        </Grid>

    </DockPanel>

</Window>
